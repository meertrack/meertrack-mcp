# fly.toml app configuration file generated for meertrack-mcp on 2026-04-23T15:39:34-05:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'meertrack-mcp'
primary_region = 'iad'

[build]

[env]
  # Base only — not the URL advertised on the wire. The served canonical URL is
  # this value's origin plus the resource identifier's path suffix (RFC 9728
  # §3.1), i.e. `…/oauth-protected-resource/mcp`, computed in
  # `src/transports/http.ts`. The resource identifier itself comes from the
  # MEERTRACK_OAUTH_AUDIENCE secret, never from here.
  MEERTRACK_MCP_PRM_URL = 'https://mcp.meertrack.com/.well-known/oauth-protected-resource'
  MEERTRACK_MCP_PUBLIC_HOST = 'mcp.meertrack.com'
  PORT = '3001'

[http_service]
  internal_port = 3001
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

  [[http_service.checks]]
    interval = '30s'
    timeout = '5s'
    grace_period = '10s'
    method = 'GET'
    path = '/health'

[[vm]]
  size = 'shared-cpu-1x'
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 256
